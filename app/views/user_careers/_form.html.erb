<%= form_for(@user_career) do |f| %>
  <% if @user_career.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_career.errors.count, "error") %> prohibited this user_career from being saved:</h2>

      <ul>
      <% @user_career.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.hidden_field :user_id, value: User.take.id %>
  </div>
    <%= f.hidden_field :career_id, value: @career.id %>
  <div class="actions">
    <%= f.submit 'Start Career Path' %>
  </div>
<% end %>
